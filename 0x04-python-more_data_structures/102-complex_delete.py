#!/usr/bin/python3

def complex_delete(a_dictionary, value):
    dkeys = list(a_dictionary.keys())

    for a in dkeys:
        if val == a_dictionary.get(a):
            del a_dictionary[a]

    return (a_dictionary)
